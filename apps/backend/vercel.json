{
  "version": 2,
  "outputDirectory": "./",
  "installCommand": "corepack enable && yarn set version berry && yarn install",
  "buildCommand": "yarn workspaces foreach -pt --all --include=packages/* run build && turbo run build --filter=@repo/backend",
  "builds": [
    {
      "src": "apps/backend/src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "HEAD", "OPTIONS"]
    }
  ]
}
