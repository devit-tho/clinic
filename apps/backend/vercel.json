{
  "version": 2,
  "installCommand": "corepack enable && yarn set version berry && yarn install --immutable",
  "buildCommand": "yarn workspaces foreach -pt --all --include=packages/* run build && yarn workspace apps/backend build",
  "outputDirectory": "apps/backend/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "main.js",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
    }
  ]
}
